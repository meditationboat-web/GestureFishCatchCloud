# 拳行街巷 · MediaPipe 手势撑地拳击

## 打开项目
用 Unity Hub 打开本目录。已在本机 Unity **2022.3.25f1** 完成编译、场景引用和物理运行验证。
原工程记录的版本是 2022.3.62f2c1；本次使用本机已安装的 2022.3.25f1 导入，ProjectVersion 已随编辑器更新。

主场景：`Assets/GestureFistGame/Scenes/GestureFistGame.unity`。双击后直接 Play，不需要先运行场景生成菜单。
场景中保存了完整角色、双拳、碰撞体、支点圈、地形、守卫、灯光、摄像机、检查点、终点、中文 Canvas 和已连接的脚本。
当前保存的场景共有 685 个对象，缺失脚本为 0；场景内容可以在未进入 Play 时直接查看和修改。
角色预制体：`Assets/GestureFistGame/Prefabs/PlayerRig.prefab`。

## 日式街道场景（2026-09-21）

已使用本机 Japanese Street 资源搭建街巷：15 栋房屋、8 根电线杆、6 台售货机、自行车、花盆、路牌、围栏、道路标线和沿街平台。主 Scene 仍是 `Assets/GestureFistGame/Scenes/GestureFistGame.unity`，无需执行生成脚本即可查看和游玩。

前半段是街道训练及 doro 守卫，中央排水沟保留跨越挑战，后半段沿台阶上行至终点。街边地面、房屋和售货机带简化碰撞。原有双拳撑地物理、检查点、doro、蓝色提示门和原生灰白 UGUI 均保留。

资源位于 `Assets/Japanese_Street`；选择性导入 152 个资产文件，约 280 MiB，纹理导入上限设为 1024，并缩小了复制的高分辨率贴图，原资源包不变。场景里的资源包演示门脚本及演示 UI 已移除，避免抢占游戏输入。模型网格和材质仍引用资产，场景实例可直接编辑。

菜单 `Gesture Fist Game/应用日式街道场景` 可重新布置环境，会替换本次生成的街区；手工修改前请另存场景。`重建完整场景` 也会应用日式街道。切换前的 doro 山谷版本已备份至 `Artifacts/before-japanese/GestureFistGame.unity.backup`。

最新检查：`Artifacts/japanese-scene-validation.txt`、`japanese-runtime.log`、`mouse-down-windows-build.log` 和 `mouse-down-windows-player.log`。当前 Windows 版本已重新构建并通过 29 项运行检查，其中包含直接下拖撑地和滚轮微调。自动鼠标验证采用输入回放，不能代替真人手感测试。

## doro 小怪与蓝色提示门

5 个小怪已使用参考工程 `My project (2)` 中的 doro FBX 和原贴图；保留追近、受击、击退、倒地、复位与通关引用。模型摆正朝向并按约 1.5 米高度对齐脚底，碰撞体随体型调整。四块提示牌改为蓝色渐变双柱与半透明幕布，保留原来的中文教学内容；只复用参考触发器的外观，不增加宠物数量增减玩法。

模型、贴图、材质与白字提示均预先保存在 Scene 中，不需要运行时生成。字体图集更新由 `GateLabelFont` 同步，文字可被不透明场景物体遮挡。原生灰白 UGUI 和鼠标默认模式保持不变。

资源位置：`Assets/GestureFistGame/Art/Doro` 与 `Art/TriggerGate`。旧场景备份为 `Artifacts/before-doro/GestureFistGame.unity.backup`。菜单 `Gesture Fist Game/更新 doro 小怪和蓝色提示门` 可以只更新这些美术对象；完整重建菜单也已使用新样式。

已选择并应用 06 日式街道。完整资源分析仍保留在上两级目录的 `场景资源可选清单.md`。

## 看得到画面，但鼠标不动 / Package Manager 登录报错

`Cannot fetch authorization code. User access token is expired or invalid` 是 Unity 在线包搜索的账号错误。本机 Console 曾开启 **Error Pause**，这类编辑器报错会把 Play 自动暂停，画面保留，但所有游戏 Update 和鼠标控制停止。

- 在 Unity Console 工具栏关闭 **Error Pause**，取消顶部暂停按钮，再点击 **Game** 窗口。在画面中按住左/右鼠标键再拖动。
- 也可使用菜单 **Gesture Fist Game → 打开主场景并恢复鼠标游玩**：关闭错误暂停和 Package Manager 窗口，打开主场景、恢复鼠标模式，并聚焦 Game。
- 工程尚未在 Unity 打开时，可双击工程根目录 `打开Unity并运行.cmd`。它使用本机安装的 Unity 2022.3.25f1。
- 直接游玩可双击 `直接运行游戏.cmd`，或下方说明的 exe，不经过 Unity Package Manager。

本次关闭 Error Pause 时保留其他 Console 设置，错误仍会正常显示。需要恢复在线包搜索时，请关闭编辑器，在 **Unity Hub 退出账号并重新登录** 后重开工程；项目代码无法刷新 Unity 账号令牌。

## 鼠标游玩（默认）
打开场景或独立程序后默认是鼠标模式，无需摄像头。左下角的模式按钮、复位按钮和操作说明都已保存在 Scene 中。
界面使用 Unity 内置 UGUI 精灵、默认按钮颜色状态与灰白配色。

1. 在游戏画面中按住左键控制左拳，按住右键控制右拳，也可以同时按住两键。
2. 按住后左右拖动调整拳头横向位置；向下拖动会同时降低并收回拳头。
3. 拳头碰到地面、支撑圈亮起后，继续向下拖动就会把身体向前并向上撑起；滚轮用于高度微调。
4. 向上拖动会抬拳并向前伸，方便搭上台阶；松开按键释放支点并收拳，保留身体惯性。
5. 反复执行“伸拳 → 下拖接地 → 继续下拖撑起 → 松开换支点”。快速向上拖动可出拳击退 doro。

竖直拖动和滚轮只影响当前按住的拳头。点击 UI 不会同时挥拳；从 UI 开始拖出也不会抓住拳头。打开说明、切换模式或失去窗口焦点会取消鼠标抓取。
左下角可以直接点击“回到检查点 / 重新开始”，整个鼠标模式不要求键盘。

- F1：手势模式；F2：键盘模式（再次按回鼠标）；F3：鼠标模式。
- 鼠标模式会暂停摄像头采集；点击“手势控制”才启动摄像头。

## 摄像头操作
先点击左下角“手势控制 F1”或按 F1。
1. 正对摄像头，退到肩膀、肘部、双手都能入镜的位置。保持约 1 秒自动校准。
2. 握拳向前下方伸出，让屏幕里的拳头实际接触地面。青色支点圈亮起表示已支撑。
3. 保持握拳，手臂向后划，身体会向前移动；向下压，身体会被撑起。
4. 抬手或张掌释放支点，再伸向下一个接触点。双拳下压后释放，角色保留惯性跃过沟壑。
5. 快速挥拳命中守卫可造成伤害、击退和倒地。经过青色地面圆盘更新检查点，沿日式街道上行，抵达金色门框通关。

- C：重新校准。识别丢失或手腕置信度太低时停止施力。
- H：显示/隐藏操作说明。
- R：回到最近检查点；通关后重新开始。
- 左下角可重试摄像头或切换设备。
- 本机检查只枚举到 Intel Virtual Camera，它输出的格式不受当前 Unity 捕获接口支持，启动时会被跳过。请接入可用的 USB 摄像头再重试；这次未完成真人摄像头手感验证。
- 拳头颜色：左手青色，右手橙红色。绿色骨架显示人体检测位置。
- 默认允许“腕部支撑模式”：距离较远、手部细节无法识别时，可靠的人体手腕位置仍可撑地。手部模型明确识别张掌时会释放。
  要强制握拳识别，在 TrackingSystem 的 FistGestureInput 勾选 requireConfirmedFist。

## 无摄像头时测试物理
按 **F2** 切换键盘测试模式，屏幕会明确显示当前模式。
按住 **Q** 选择左拳、**E** 选择右拳，可同时按住：
- W/S：拳头前后；A/D：拳头左右。
- 上/下箭头：拳头上下。
- Z/X：左/右拳出拳；松键收回。
- 空格：释放两拳支点，保留当前惯性。
- 按 F1 回到摄像头控制，按 F3 回到鼠标模式。

键盘也是控制拳头的相对位置，通过同一套接触与支撑物理推动身体；没有直接的角色方向移动或空中跳跃按钮。
入门动作：按 F2，再同时按住 Q、E，用下箭头让拳头接地；支撑圈亮起后按 S 向后划拳，身体向前。抬拳、向前伸，再次落拳，循环前进。遇到台阶要先抬高手臂，搭到台阶上表面再下压。

## 核心实现
- MediaPipeTracker：一路 WebCamTexture 输入，CPU Pose Landmarker Lite + 双手 Hand Landmarker。
  从 StreamingAssets 用托管 File.ReadAllBytes 读取模型，再以 modelAssetBuffer 创建任务，解决 Windows 原生路径含中文的问题。
  两个任务使用各自的 Image 包装实例，像素缓冲复用；识别结果在 Unity 主线程消费。
- FistGestureInput：肩宽校准，肩膀中心相对手腕位置、有限深度、握拳判断、左右手空间匹配、平滑、置信度与过期处理。
- MouseFistInput：左右鼠标按键独立选择拳头，拖动与滚轮转为相对手部目标；复用相同的接触、支撑和攻击物理。处理 UI 点击隔离、释放与焦点丢失。
- FistAnchor：球体扫掠碰撞；只在真实接触时建立支点；抬手、张掌、超出臂长、失去识别时释放。拳头扫掠命中守卫，每次有效挥动不会因多个碰撞体重复伤害。
- FistPlayerController：目标身体位置 = 支点位置 − 当前拳头相对位置。PD 弹簧施力、有速度/加速度上限，离地释放保留惯性。
- FistGameManager：检查点、掉落、关卡阶段、终点、重开。
- TrainingDummy：追近、受击、击退、倒地和恢复。
- FistGameUI / GestureOverlay / CameraFollow：中文界面、预览骨架、摄像机跟随。

在 Inspector 中可调手臂放大倍率、深度倍率、置信度、支撑弹簧、阻尼、最大速度、臂长、出拳阈值等。
单目摄像头的前后深度是模型估计，需要结合实际摄像头距离调整 depthScale；真人操作舒适度仍需现场校准。

## 工程与依赖
插件仍使用提供工程内的 `Packages/com.github.homuler.mediapipe`，模型在原有 StreamingAssets 中。
Windows 使用 CPU 推理；没有要求外部 Python 或另装 MediaPipe。
为解决原 manifest 的非必要 2D/跨平台工具包下载失败，保留了可运行的依赖集，原配置备份在 `Packages/manifest.json.original`。
原插件示例 Scene 保留；新的主场景列在 Build Settings 第一项。
中文字体 Noto Sans CJK SC 随项目提供，许可见 `Assets/GestureFistGame/Fonts/LICENSE.txt`。

## 验证记录
最终 Windows 可执行程序已实际启动，29 项运行检查全部通过，退出码为 0；最后一轮仅靠模拟手臂的撑地轨迹，16 次撑地进入终点。
验证包含真实原生模型加载及空白图推理、接触支撑、移动与抬升、惯性释放、攻击击退、检查点、掉落、通关停留、重开和完整路线。
新增鼠标输入回放覆盖独立选拳、UI 点击隔离、双拳接地、拖动推进、滚轮抬升、释放惯性、出拳，以及场景模式按钮。
这不代表已验证真人摄像头识别准确率或舒适度。

`Artifacts/scene-validation.txt`：已保存对象和引用检查。
`Artifacts/physics-validation.txt`：原生模型创建与推理、双拳支撑、惯性、碰撞攻击、掉落/终点/重开、完整撑地路线测试。
`Artifacts/player-validation.txt`：最终 Windows 程序的 29 项检查结果与每次撑地的位置，其中包含直接下拖撑地和滚轮微调检查。
`Artifacts/japanese-windows-player.log`：含鼠标与原生 UI 的最终可执行程序运行日志。
`Artifacts/camera-validation.txt`：本机摄像头检查结果（以该文件为准）。
`Artifacts/player-camera-validation.txt`：Windows 程序摄像头启动检查结果。
`Artifacts/game-preview.png`：Unity 实际渲染截图。
`Artifacts/mouse-preview.png` / `Artifacts/mouse-help-preview.png`：更新后的鼠标模式及操作说明界面截图。
`Artifacts/最终验收.md`：交付状态与验证范围。

自动关卡验证通过 SubmitHand 输入模拟手臂轨迹，走完整个场地；它验证物理与关卡连通性，不能替代真人摄像头手感测试。

## 编辑与构建
所有场景物体可直接编辑。菜单 `Gesture Fist Game/重建完整场景` 是开发辅助，会覆盖本次新增主场景与角色预制体；手工调整场景后请先另存副本。
菜单 `Gesture Fist Game/添加鼠标控制与原生界面` 只更新输入组件和 Canvas，保留已有角色与关卡。
程序入口可用 Unity Play，或直接双击 Windows 构建输出 `Builds/Windows/GestureFistGame.exe`。
拷贝可运行版时请复制整个 `Builds/Windows` 文件夹，保留 exe、Data、MonoBleedingEdge 和所有 DLL 的相对位置。
关闭独立游戏窗口可用右上角关闭按钮或 Alt+F4。

